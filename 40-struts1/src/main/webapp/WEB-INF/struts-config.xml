<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE struts-config PUBLIC
        "-//Apache Software Foundation//DTD Struts Configuration 1.3//EN"
        "http://struts.apache.org/dtds/struts-config_1_3.dtd">

<struts-config>

    <form-beans>
        <form-bean name="saludoForm" type="com.cursosdedesarrollo.SaludoForm"/>
    </form-beans>

    <action-mappings>
        <!--
          validate="false": la validación la hace el propio Action,
          lo que permite mostrar el formulario vacío en el primer GET
          sin que Struts intercepte con errores de validación.
        -->
        <action path="/saludo"
                type="com.cursosdedesarrollo.SaludoAction"
                name="saludoForm"
                scope="request"
                validate="false">
            <forward name="entrada" path="/WEB-INF/vistas/entrada.jsp"/>
            <forward name="exito"   path="/WEB-INF/vistas/resultado.jsp"/>
        </action>
    </action-mappings>

    <message-resources parameter="ApplicationResources"/>

</struts-config>
